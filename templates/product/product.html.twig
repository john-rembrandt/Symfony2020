<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        {% block title %}Hello ProductController!{% endblock %}
        <link rel="stylesheet" href="..\Css\style.css" type="text/css" />
    </head>
        <body>
            {% block body %}
            <style>
            .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
            .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
            </style>

                <div class="example-wrapper">
                {#  <h1>Hello {{ controller_name }}</h1>
                    
                    <p>Your new id is : {{ id }}</p>
                    <p>Your new name is : {{ name }}</p>
                    <p>Your new price is : {{ price }}</p> #}
                    
                    
                    {# Your new Tab name is : {{ attribut }} #}
                    {% for product in products %}
                    <article class="article">
                        <p>Identifiant integer de la db : <strong> {{ product.id  }} </strong></p>
                        <p>Nom venant de Faker : <h2> {{ product.name  }} </h2> </p>
                        <p>Prix du produit : <h3> {{ product.price  }} </h3> </p>
                    </article>
                    {% endfor %}
    
                    
                    <ul>
                        <li>Your controller at <code>
                        <a href="{{ 'C:/wamp64/www/Symfony2020/src/Controller/ProductController.php'|file_link(0) }}">src/Controller/ProductController.php</a>
                        </code>
                        </li>
                        
                        <li>Your template at <code>
                        <a href="{{ 'C:/wamp64/www/Symfony2020/templates/product/product.html.twig'|file_link(0) }}">templates/product/index.html.twig</a>
                        </code>
                        </li>
                    </ul>
                </div>
            {% endblock %}
        </body>
</html>
